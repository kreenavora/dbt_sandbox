sources:
  - name: tpch_now
    database: raw
    schema: tpch_now

    config:
      freshness:
        warn_after: {count: 24, period: hour}
        error_after: {count: 48, period: hour}
      loaded_at_field: _etl_loaded_at

    tables:

    - name: lineitem

    - name: orders

  - name: tpch_sf001
    database: raw
    schema: tpch_sf001

    config:
      freshness:
        warn_after: {count: 24, period: hour}
        error_after: {count: 48, period: hour}
      loaded_at_field: _etl_loaded_at

    tables:
    - name: partsupp

    - name: part

    - name: supplier